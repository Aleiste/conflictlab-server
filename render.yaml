# render.yaml - Configuration pour Render.com
services:
  - type: web
    name: conflictlab-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
